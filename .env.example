# Copy to .env and set a strong password before exposing the stack publicly.
# Used by docker-compose.yml for both the Postgres container and the server's
# connection string.
POSTGRES_PASSWORD=somesecurepassword
